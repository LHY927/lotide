/**
 * Return the last variable of the array.
 *
 * @param {Array} input The target array.
 * @return {var} The last variable.
 */
const tail = function(input){
    return input.slice(1);
}

module.exports = tail;